# Done Means Verified Hook Ideas

Future hooks may warn when:

- A final response says `done`, `fixed`, `works`, or `ready` without checks.
- A report has no proof labels.
- A report omits `not proven`.
- Browser, provider, extension, or release claims are made from source-only checks.

No hook is implemented or active in this folder.

