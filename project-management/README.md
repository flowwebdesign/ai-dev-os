# Project Management Templates

These files give agents and humans a small operating layer for project truth.

Use them inside `.ai-dev/` for:

- current active state
- ordered next actions
- decisions with reasons
- risks and repeated failure patterns
- progress log
- weekly review
- machine-readable run cards

The templates are intentionally plain Markdown and JSON so they work without a toolchain.
